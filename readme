The data directory is where the database is stored.

To connect:
[postgres@server]$ psql -d scoreboard

To show summary information about all tables in database:
=> \dt

To exit:
=> \q

To run SQL script:
\i scriptName.sql
